---
import HomeLink from "../ui/HomeLink.astro";
import Layout from "../layouts/Layout.astro";
import Tooltip from "../ui/Tooltip.astro";
import Toolbar from "../scenes/leg-builder/Toolbar.astro";
import ViewportChrome from "../scenes/leg-builder/ViewportChrome.astro";
import RaceControls from "../scenes/leg-builder/RaceControls.astro";
import ObjectInspector from "../scenes/leg-builder/ObjectInspector.astro";
import OptionsSidebar from "../scenes/leg-builder/OptionsSidebar.astro";
import "../scenes/leg-builder/leg-builder.css";
---

<Layout>
  <main
    id="leg-builder"
    data-sidebar-open="false"
    data-previewing="false"
    class="relative size-full overflow-hidden"
  >
    <HomeLink id="builder-home-link" />
    <p
      id="builder-error"
      class="pointer-events-none absolute left-1/2 top-24 z-30 -translate-x-1/2 rounded-md border border-rose-900 bg-rose-950/90 px-3 py-2 font-mono text-xs text-rose-200 shadow-lg backdrop-blur empty:hidden"
    >
    </p>

    <Toolbar />

    <p
      id="tool-hint"
      data-role="tool-hint"
      aria-live="polite"
      class="pointer-events-none absolute inset-x-0 top-16 z-20 mx-auto w-max whitespace-nowrap text-center text-xs text-zinc-500"
    >
      Hold <kbd>Space</kbd> and drag to pan
    </p>

    <ViewportChrome />

    <RaceControls />

    <Tooltip id="builder-tooltip" />

    <button
      id="toggle-sidebar"
      type="button"
      aria-controls="leg-options-sidebar"
      aria-expanded="false"
      aria-label="Open options sidebar"
      data-tooltip="Open Sidebar"
      class="absolute right-3 top-3 z-30 flex size-9 items-center justify-center rounded-md border border-zinc-700 bg-zinc-800 text-zinc-200 shadow-lg hover:bg-zinc-700 hover:text-white"
    >
      <svg
        data-sidebar-icon="open"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        class="size-5"
        aria-hidden="true"
      >
        <rect x="3" y="3" width="18" height="18" rx="2"></rect>
        <path d="M15 3v18"></path>
      </svg>
      <svg
        data-sidebar-icon="close"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        class="hidden size-5"
        aria-hidden="true"
      >
        <path d="m18 6-12 12M6 6l12 12"></path>
      </svg>
    </button>

    <ObjectInspector />

    <OptionsSidebar />

    <div id="builder-workspace" class="absolute inset-0 overflow-hidden">
      <canvas
        id="gl-canvas"
        class="size-full bg-zinc-900 data-[pointer=pan]:cursor-grab data-[pointer=select]:cursor-default data-[pointer=shape]:cursor-crosshair"
      >
        Canvas not supported
      </canvas>
      <div id="builder-grid" data-role="grid-overlay" aria-hidden="true"></div>
      <canvas
        id="editor-overlay"
        data-role="editor-overlay"
        class="pointer-events-none absolute inset-0 z-10 size-full"
        aria-hidden="true"></canvas>
    </div>
  </main>
</Layout>

<script>
  import { mountLegBuilderPage } from "../scenes/leg-builder/mountPage";
  mountLegBuilderPage();
</script>
