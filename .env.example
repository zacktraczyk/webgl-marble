# Use console locally, posthog on configured Vercel preview/production builds,
# or off to disable analytics entirely.
PUBLIC_ANALYTICS_MODE=console
PUBLIC_ANALYTICS_ENVIRONMENT=development

# Browser project tokens are public identifiers, not personal API keys.
PUBLIC_POSTHOG_PROJECT_TOKEN=
PUBLIC_POSTHOG_HOST=
